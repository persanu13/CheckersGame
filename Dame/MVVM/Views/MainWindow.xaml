<Window x:Class="Dame.MVVM.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:Dame.MVVM.Views"
        mc:Ignorable="d"
        Title="CheckerS"
        Height="778" Width="1080"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
        <Window.InputBindings>
        <KeyBinding Key="Escape" Command="{Binding EscPressCommand}"/>
        </Window.InputBindings>
    <Grid>
        <ContentControl Content="{Binding NavigationService.CurrentWindowView}"/>
    </Grid>
</Window>
